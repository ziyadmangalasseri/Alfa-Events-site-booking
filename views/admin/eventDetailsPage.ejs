<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Event List</title>
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-image: url("background.jpg");
        background-size: cover;
        background-position: center;
      }
      .list-container {
        background-color: rgba(0, 0, 0, 0.7);
        color: white;
      }
      .event-item {
        background-color: rgba(255, 255, 255, 0.1);
        border-radius: 0.5rem;
        padding: 1rem;
        margin-bottom: 0.5rem;
      }
      .btn {
        color: white;
      }
    </style>
  </head>
  <body>
    <div
      class="container d-flex justify-content-center align-items-center min-vh-100"
    >
      <div class="list-container p-5 rounded-4 shadow-lg w-100" style="max-width: 600px;">
        <h3 class="text-center mb-4">Event Details</h3>

        
        <div id="eventList">
            <% if (event){ %>
                  <div class="event-item d-flex justify-content-between align-items-center mb-3 p-3" style="background-color: rgba(255, 255, 255, 0.1); border-radius: 0.5rem;">
                    <!-- Wrap event place and date in clickable links -->
                    <a href="/eventDetail/<%= event._id %>" class="text-decoration-none text-white">
                      <h6>Place :  <%= event.place %></h6>
                      <h6>Date : <%= event.formattedDate %></h6>
                      <h6>Reporting time :  <%= event.reportingTime %></h6>
                      <h6>Exit time :  <%= event.exitTime %></h6>
                      <h6>Job title :  <%= event.jobTitle %></h6>
                      <h6>Job description : <%= event.jobDescription %></h6>
                      <h6>Employer limit : <%= event.employerLimit %></h6>
                      <h6>Expiration time : <%= event.formattedexpirationTime %></h6>
                    </a>
                  </div>
              <% } else { %>
                <p>No events available.</p>
              <% } %>
        </div>
        <div class="d-flex justify-content-between">
        <form action="">
          <button type="submit" class="btn btn-success modal-btn">Edit</button>
        </form>
        <form action="">
          <button type="submit" class="btn btn-danger modal-btn">Delete</button>
        </form>
      </div>
      </div>
    </div>
  </body>
</html>